<form method="post" action="{{ path('app_vip_delete', {'id': vip.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ vip.id) }}">
    <button class="btn">Delete</button>
</form>
